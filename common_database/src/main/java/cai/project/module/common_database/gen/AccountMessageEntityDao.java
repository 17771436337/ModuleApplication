package cai.project.module.common_database.gen;

import java.util.List;
import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;
import org.greenrobot.greendao.query.Query;
import org.greenrobot.greendao.query.QueryBuilder;

import cai.project.module.common_database.entity.account.AccountMessageEntity;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "ACCOUNT_MESSAGE_ENTITY".
*/
public class AccountMessageEntityDao extends AbstractDao<AccountMessageEntity, Long> {

    public static final String TABLENAME = "ACCOUNT_MESSAGE_ENTITY";

    /**
     * Properties of entity AccountMessageEntity.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Name = new Property(1, String.class, "name", false, "NAME");
        public final static Property Detail = new Property(2, String.class, "detail", false, "DETAIL");
        public final static Property AccountId = new Property(3, Long.class, "accountId", false, "ACCOUNT_ID");
        public final static Property SORTNO = new Property(4, int.class, "SORTNO", false, "SORTNO");
    }

    private Query<AccountMessageEntity> accountEntity_AccountMessagesQuery;

    public AccountMessageEntityDao(DaoConfig config) {
        super(config);
    }
    
    public AccountMessageEntityDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"ACCOUNT_MESSAGE_ENTITY\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: id
                "\"NAME\" TEXT NOT NULL ," + // 1: name
                "\"DETAIL\" TEXT NOT NULL ," + // 2: detail
                "\"ACCOUNT_ID\" INTEGER NOT NULL ," + // 3: accountId
                "\"SORTNO\" INTEGER NOT NULL );"); // 4: SORTNO
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"ACCOUNT_MESSAGE_ENTITY\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, AccountMessageEntity entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getName());
        stmt.bindString(3, entity.getDetail());
        stmt.bindLong(4, entity.getAccountId());
        stmt.bindLong(5, entity.getSORTNO());
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, AccountMessageEntity entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
        stmt.bindString(2, entity.getName());
        stmt.bindString(3, entity.getDetail());
        stmt.bindLong(4, entity.getAccountId());
        stmt.bindLong(5, entity.getSORTNO());
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public AccountMessageEntity readEntity(Cursor cursor, int offset) {
        AccountMessageEntity entity = new AccountMessageEntity( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.getString(offset + 1), // name
            cursor.getString(offset + 2), // detail
            cursor.getLong(offset + 3), // accountId
            cursor.getInt(offset + 4) // SORTNO
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, AccountMessageEntity entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setName(cursor.getString(offset + 1));
        entity.setDetail(cursor.getString(offset + 2));
        entity.setAccountId(cursor.getLong(offset + 3));
        entity.setSORTNO(cursor.getInt(offset + 4));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(AccountMessageEntity entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(AccountMessageEntity entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(AccountMessageEntity entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "accountMessages" to-many relationship of AccountEntity. */
    public List<AccountMessageEntity> _queryAccountEntity_AccountMessages(Long accountId) {
        synchronized (this) {
            if (accountEntity_AccountMessagesQuery == null) {
                QueryBuilder<AccountMessageEntity> queryBuilder = queryBuilder();
                queryBuilder.where(Properties.AccountId.eq(null));
                queryBuilder.orderRaw("T.'SORTNO' ASC");
                accountEntity_AccountMessagesQuery = queryBuilder.build();
            }
        }
        Query<AccountMessageEntity> query = accountEntity_AccountMessagesQuery.forCurrentThread();
        query.setParameter(0, accountId);
        return query.list();
    }

}
